
{{#if places}}

    {{#each places as |place|}}

<div class="card" id="{{_id}}"><span class="cardtitle noselect"><h4>{{place.name}}</h4></span>
  <h5>{{place.country}}</h5>
  <button class="delete-place" data-deleteid="{{place._id}}">click me</button>

</div>
{{/each}}
{{/if}}
